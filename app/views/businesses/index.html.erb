<p id="notice"><%= notice %></p>

<h1>Listing Businesses</h1>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th>Level</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @businesses.each do |business| %>
      <tr>
        <td><%= business.name %></td>
        <td><%= business.address %></td>
        <td><%= business.phone %></td>
        <td><%= business.website %></td>
        <td><%= business.level %></td>
        <td><%= link_to 'Show', business, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_business_path(business), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', business, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Business', new_business_path %>
